#include <stdio.h>
/**
 * main . A programme that print a line using printf function
 * return: 0 (success)
 */
int main(void)
{
	printf["with proper grammar, but the outcome is a piece of art,\n]
	Return(0);
}
